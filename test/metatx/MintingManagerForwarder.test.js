const { ethers } = require('hardhat');
const { expect } = require('chai');

const { sign } = require('../helpers/metatx');

const { BigNumber } = ethers;

describe('MintingManagerForwarder', () => {
});
